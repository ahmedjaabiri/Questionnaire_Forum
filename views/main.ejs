<!DOCTYPE html>
<html>

<head>
  <title>Dashboard</title>
  <link rel="stylesheet" href="/assets/semantic/semantic.min.css" />
  <style>
    .main.container {
      margin-top: 7em;
    }

    .inputfile {
      width: 0.1px;
      height: 0.1px;
      opacity: 0;
      overflow: hidden;
      position: absolute;
      z-index: -1;
    }
  </style>
</head>

<body style="padding-left: 15%; padding-right: 15%; padding-bottom: 10%;">
  <h2>Bienvenue</h2>
  <div class="field">
    <label for="Nomcateg">Voulez-vous créer un nouveau formulaire</label>
  </div>
  <a href="/add" class="ui blue labeled submit icon button">
    <i class="icon add"></i>créer</a>
  <div class="field">
    <label for="Nomcateg">Voulez-vous répondre à un formulaire existant</label>
    <table class="ui inverted grey table">
      <thead>
        <tr>
          <th>Nom formulaire</th>
          <th>Date</th>
          <th>Nombre de réponses</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% for( res in result){ %>
        <tr>
          <td><%=result[res].name%></td>
          <% ti=((result[res]._id).getTimestamp()).toString().replace(' GMT+0100 (West Africa Standard Time)','')
            %>
          <td>
            <%=ti%>
          </td>
          <%if(result[res].reponse){%>
          <td><%=result[res].reponse.length%></td>
          <%}else{%>
          <td>0</td>
          <%}%>
            <td class="collapsing">
              <div class="blue ui buttons">
                <a href="/rep?id=<%=result[res]._id%>"
          ><button id="btnedit" class="ui labled icon button">
            <i class="edit icon"></i>répondre
          </button>
          </a>
  </div>
  </td>
  </tr>
  <%}%>
          <tr>
            <td colspan="2"></td>
            <td>Nb formulaires:</td>
            <td><%= result.length %></td>
  </tr>
  </tbody>
  </table>
  </div>
</body>

</html>